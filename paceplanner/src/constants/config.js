/** Klucz do localStorage dla stanu aplikacji (użyjemy w innym story). */
export const PERSISTENCE_STORAGE_KEY = 'pacePlannerStateV3';

/** Limit całego biegu w sekundach (6:15:00) — użyte np. w projekcjach. */
export const TOTAL_RACE_LIMIT_SECONDS = 6 * 3600 + 15 * 60;